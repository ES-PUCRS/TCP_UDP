/*
 *   This package is used to provide library that would
 *   be helpfull on both implementations as Server and
 *	 Client due to prevent code replication.
 *
 *   This package WILL NOT contain any business law that
 *   is apply on those applications.
 */
package libs;